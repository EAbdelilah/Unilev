[profile.default]
src = 'src'
out = 'out'
libs = ['lib']
gas_reports = ["*"]
optimizer = true
optimizer_runs = 200
solc = "0.8.27"
via_ir = true
# Default fork = Polygon mainnet (used by legacy V3 TestSetup-based tests)
eth_rpc_url = "https://polygon-mainnet.g.alchemy.com/v2/MShMmpJbY-27CEbyan4Ac"

remappings = [
    '@uniswap/v4-core/=lib/v4-core/',
]

fs_permissions = [
    { access = "read-write", path = "." },
    { access = "read-write", path = "C:/Users/faar_/AppData/Local/Temp/opencode" },
]

[profile.default.invariant]
runs = 8
depth = 4

# V4 profile: no default fork (V4 tests create their own forks via vm.createSelectFork)
[profile.v4]
src = 'src'
out = 'out'
libs = ['lib']
optimizer = true
optimizer_runs = 200
solc = "0.8.27"
via_ir = true
remappings = [
    '@uniswap/v4-core/=lib/v4-core/',
]
fs_permissions = [
    { access = "read-write", path = "." },
    { access = "read-write", path = "C:/Users/faar_/AppData/Local/Temp/opencode" },
]

[profile.v4.invariant]
runs = 8
depth = 4